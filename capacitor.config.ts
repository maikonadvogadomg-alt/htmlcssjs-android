import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.github.htmlcssjs',
  appName: 'htmlcssjs',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};
export default config;
